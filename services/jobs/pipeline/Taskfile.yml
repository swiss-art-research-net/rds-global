# https://taskfile.dev

version: '3'

vars:
  DATASETS_CONFIG_YML: /config/datasets.yml
  DIRECTORY_INGEST_DATA: /data/ingest
  DIRECTORY_SOURCE_DATA: /data/source
  DIRECTORY_SAMEAS_STATEMENTS: /data/sameAsStatements/sources
  WIKIDATA_IDS_CSV: /config/wikidata/wikidata-external-id-properties.csv

env:
  QLEVER_ENDPOINT: http://qlever:7001
  WIKIDATA_ENDPOINT: https://qlever.dev/api/wikidata

includes:
  utilities:
    taskfile: ./taskfiles/utilities.yml
    flatten: true
tasks:

  default:
    desc: Run entire pipeline
    interactive: True
    cmds:
      - task: update-data
      - task: prepare-metadata-for-indexing
      - task: index-data
      - task: restart-qlever
      - task: generate-type-mappings
      - task: generate-labels
      - task: generate-sameas-statements
      - task: index-data
      - task: restart-qlever
      - task: add-data-to-search-index

  add-data-to-search-index:
    desc: Add all data to OpenSearch index
    vars:
      DATASETS:
        sh: echo "$DATASETS"
    cmds:
      - for:
          var: DATASETS
          split: ','
          as: DATASET
        task: _add_to_search_index
        vars:
          DATASET: "{{.DATASET}}"

  fetch-all-sameas-statements:
    desc: Fetch data reuired for SameAs statements
    vars:
      DATASETS:
        sh: echo "$DATASETS"
    cmds: 
      - mkdir -p {{.DIRECTORY_SAMEAS_STATEMENTS}}
      - for:
          var: DATASETS
          split: ','
          as: DATASET
        task: fetch-sameas-statements-for-dataset
        vars:
          DATASET: "{{.DATASET}}"    
      - for:
          var: DATASETS
          split: ','
          as: DATASET
        task: fetch-sameas-statements-for-dataset-from-wikidata
        vars:
          DATASET: "{{.DATASET}}"

  fetch-sameas-statements-for-dataset:
    desc: Fetch SameAs statements for a specified dataset passed as DATASET variable or via CLI argument
    vars:
      DATASET:
        sh: |
          if [ -z "{{.DATASET}}" ]; then
            echo "{{.CLI_ARGS}}"
          else
            echo "{{.DATASET}}"
          fi
    cmds:
      - task: _fetch-sameas-statements-for-dataset
        vars:
          DATASET: "{{.DATASET}}"

  fetch-sameas-statements-for-dataset-from-wikidata:
    desc: Fetch SameAs statements from Wikidata for a specified dataset passed as DATASET variable or via CLI argument
    status:
      - test -f "{{.DIRECTORY_SAMEAS_STATEMENTS}}/{{.DATASET}}WikidataSameAs.ttl"
    vars:
      DATASET:
        sh: |
          if [ -z "{{.DATASET}}" ]; then
            echo "{{.CLI_ARGS}}"
          else
            echo "{{.DATASET}}"
          fi
    cmds:
      - python /pipeline/scripts/fetchMatchesFromWikidata.py --endpoint "{{.QLEVER_ENDPOINT}}" --wikidata-endpoint "{{.WIKIDATA_ENDPOINT}}" --wikidata-properties-csv "{{.WIKIDATA_IDS_CSV}}" --output-directory "{{.DIRECTORY_SAMEAS_STATEMENTS}}" --config "{{.DATASETS_CONFIG_YML}}" --dataset "{{.DATASET}}"

  generate-sameas-statements:
    desc: Generates SameAs statements between entities
    vars:
      DATA_DIRECTORY: /data/sameAsStatements/combined
      FILE_FORMAT: ttl
      GRAPH: http://schema.swissartresearch.net/rds/exact-match-statements
    cmds:
      - task: fetch-all-sameas-statements
      - mkdir -p {{.DATA_DIRECTORY}}
      - task: process-sameas-statements
      - task: _prepareForIndexing
        vars:
          DIRECTORY: "{{.DATA_DIRECTORY}}"
          FILE_FORMAT: "{{.FILE_FORMAT}}"
          GRAPH: "{{.GRAPH}}"
  
  generate-type-mappings:
    desc: Generate type mappings for RDS entities
    vars:
      DATASETS:
        sh: echo "$DATASETS"
      OUTPUT_DIRECTORY: /data/ingest/type-mappings
    cmds:
      - mkdir -p "{{.OUTPUT_DIRECTORY}}"
      - for:
          var: DATASETS
          split: ','
          as: DATASET
        cmd: python /pipeline/scripts/generateTypeMappings.py --endpoint "{{.QLEVER_ENDPOINT}}" --config "{{.DATASETS_CONFIG_YML}}" --dataset "{{.DATASET}}" --output-directory "{{.OUTPUT_DIRECTORY}}" {{.CLI_ARGS}}

  generate-labels:
    desc: Generate labels for URIs
    vars:
      DATASETS:
        sh: echo "$DATASETS"
      OUTPUT_DIRECTORY: /data/ingest/labels
    cmds:
      - mkdir -p {{.OUTPUT_DIRECTORY}}
      - for:
          var: DATASETS
          split: ','
          as: DATASET
        cmd: python /pipeline/scripts/generateLabels.py --endpoint "{{.QLEVER_ENDPOINT}}" --config "{{.DATASETS_CONFIG_YML}}" --dataset "{{.DATASET}}" --output-directory "{{.OUTPUT_DIRECTORY}}" {{.CLI_ARGS}}

  ingest-data-from-folder:
    desc: Ingest data to QLever from a specified folder. The folder should be passed via CLI argument
    vars:
      DIRECTORY:
        sh: echo "{{.CLI_ARGS}}"
    cmds:
      - |
        if [ -z "{{.DIRECTORY}}" ]; then
          echo "Error: No directory specified. Please provide a directory as a CLI argument."
          exit 1
        fi
      - task: _ingest-data-from-folder
        vars:
          DIRECTORY: "{{.DIRECTORY}}"

  index-data:
    desc: Index all data in QLever
    cmds:
      - task: _build_qlever_index
        vars:
          INPUT_DIR: /data/ingest
          NAME: rds
    
  prepare-metadata-for-indexing:
    desc: Prepare RDS metadata for indexing
    sources:
      - /data/rds-metadata/_datasetsMetadata.ttl
      - /data/rds-metadata/rds-ontologies-description.ttl
      - /data/rds-metadata/type-mapping.ttl
    cmds:
      - task: _prepareFileForIndexing
        vars: 
          FILE: /data/source/rds-metadata/_datasetsMetadata.ttl
          FILE_FORMAT: ttl
          GRAPH: http://rds.swissartresearch.net/graph/datasetMetadata
      - task: _prepareFileForIndexing
        vars:
          FILE_FORMAT: ttl
          FILE: /data/source/rds-metadata/rds-ontologies-description.ttl
          GRAPH: http://rds.swissartresearch.net/graph/rdsOntologies
      - task: _prepareFileForIndexing
        vars:
          FILE_FORMAT: ttl
          FILE: /data/source/rds-metadata/type-mapping.ttl
          GRAPH: http://schema.swissartresearch.net/rds/typeMapping
   
  prepare-indexing:
    desc: Load values from the config yml and pass on to _prepareForIndexing. Dataset name should be passed via DATASET variable or as CLI argument
    vars:
      DATASET:
        sh: |
          if [ -z "{{.DATASET}}" ]; then
            echo "{{.CLI_ARGS}}"
          else
            echo "{{.DATASET}}"
          fi
    cmds:
      - task: _prepareForIndexing
        vars:
          DIRECTORY: "{{.DIRECTORY_SOURCE_DATA}}/{{.DATASET}}-data"
          GRAPH:
            sh: yq -r ".datasets.{{.DATASET}}.graph" "{{.DATASETS_CONFIG_YML}}"
  
  process-sameas-statements:
    desc: Process SameAs statements
    sources:
      - /data/sameAsStatements/*.ttl
    cmds:
      - mkdir -p /data/sameAsStatements/combined
      - rm -rf /pipeline/scripts/sameAsProcessing/processing_db
      # if node_modules folder does not exist, e.g because we are using a bind mount, install dependencies
      - |
        if [ ! -d "/pipeline/scripts/sameAsProcessing/node_modules" ]; then
          echo "Installing dependencies for SameAs statements processing..."
          cd /pipeline/scripts/sameAsProcessing && npm install
        fi
      - cd /pipeline/scripts/sameAsProcessing; npm run start

  remove-dataset-from-search-index:
    desc: Remove a dataset from OpenSearch index. Dataset name should be passed via DATASET variable or as CLI argument
    vars:
      DATASET:
        sh: |
          if [ -z "{{.DATASET}}" ]; then
            echo "{{.CLI_ARGS}}"
          else
            echo "{{.DATASET}}"
          fi
    cmds:
      - task: _removeDatasetFromSearchIndex
        vars:
          DATASET: "{{.DATASET}}"
  
  update-data:
    desc: Update RDF datasets
    interactive: true
    vars:
      DATASETS:
        sh: echo "$DATASETS"
    cmds:
      - for:
          var: DATASETS
          split: ','
          as: DATASET
        task: update-individual-dataset
        vars:
          DATASET: "{{.DATASET}}"

  restart-qlever:
    desc: Request a restart of the QLever service
    cmds:
      - echo "Requesting QLever restart"
      - touch /index/.restart-qlever

  update-individual-dataset:
    desc: Update individual dataset. Dataset name should be passed via DATASET variable or as CLI argument
    vars:
      DATASET:
        sh: |
          if [ -z "{{.DATASET}}" ]; then
            echo "{{.CLI_ARGS}}"
          else
            echo "{{.DATASET}}"
          fi
      DATA_DIRECTORY:
        sh: echo "{{.DIRECTORY_SOURCE_DATA}}/{{.DATASET}}-data"
    cmds:
      - |
        python /pipeline/scripts/download_datasets.py \
          --dataset "{{.DATASET}}" \
          --data_directory "{{.DATA_DIRECTORY}}" \
          --config "{{.DATASETS_CONFIG_YML}}" \
          --github-username "$GITHUB_USERNAME_SIKART" \
          --github-token "$GITHUB_TOKEN_SIKART"

      - task: prepare-indexing
        vars:
          DATASET: "{{.DATASET}}"


  verify-data:
    desc: Verify the validity of the ingest data or any data in a specified directory passed as CLI argument
    vars:
      DATA_DIRECTORY:
        sh: '[ -d "{{.CLI_ARGS}}" ] && echo "{{.CLI_ARGS}}" || echo "/data/ingest"'
    cmds:
      - |
        DATA_DIR="{{.DATA_DIRECTORY}}"
        FILES_VERIFIED=0
        FILES_OK=0
        FILES_ERROR=0
        ERROR_REPORT=""

        for file in $(find "$DATA_DIR" -type f -regex ".*\.\(ttl\|rdf\|rdfs\|nt\|nq\)"); do
          echo "Verifying $file,..."

          if out=$(rapper -g -c "$file" 2>&1); then
            triples=$(printf "%s\n" "$out" | awk '/Parsing returned/ {print $4}')
            echo "OK! Returned ${triples} triples"
            FILES_OK=$((FILES_OK + 1))
          else
            echo "VERIFICATION FAILED"
            err_line=$(printf "%s\n" "$out" | grep -i 'Error' | head -n1 || true)
            [ -z "$err_line" ] && err_line=$(printf "%s\n" "$out" | head -n1)
            echo "Error: $err_line"
            FILES_ERROR=$((FILES_ERROR + 1))
            ERROR_REPORT="${ERROR_REPORT}\n${file}: ${err_line}"
          fi
        done

        FILES_VERIFIED=$((FILES_OK + FILES_ERROR))

        echo
        echo "Verification completed."
        echo "Files verified: $FILES_VERIFIED"
        echo "Files OK: $FILES_OK"
        echo "Files with errors: $FILES_ERROR"

        if [ "$FILES_ERROR" -gt 0 ]; then
          echo
          echo "Error Report:"
          printf "%b\n" "${ERROR_REPORT#\\n}"
        fi

        if [ "$FILES_ERROR" -gt 0 ]; then
          exit 1
        fi
