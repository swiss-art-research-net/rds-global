services:
  opensearch-connector:
    ports:
      - 127.0.0.1:${PORT_OPENSEARCH_CONNECTOR:-8000}:8000

  opensearch-dashboard:
    ports:
      - 127.0.0.1:${PORT_OPENSEARCH_DASHBOARD:-5601}:5601

  opensearch-node:
    ports:
      - 127.0.0.1:${PORT_OPENSEARCH:-9200}:9200
  
  platform:
    ports:
      - ${PORT_RDS:-8080}:8080
  qlever:
    ports:
      - ${PORT_QLEVER_RDS:-7001}:7001for now,